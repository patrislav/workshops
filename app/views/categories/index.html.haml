%h1 Listing categories

%table.table
  %tr
    %th Name
    %th
    %th
    %th

  - categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'Show', category
      %td= link_to 'Edit', edit_category_path(category) if signed_in? and current_user.admin?
      %td= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } if signed_in? and current_user.admin?

%br

= link_to 'New Category', new_category_path if signed_in? and current_user.admin?
